# Milestone 2 — Hardware for AI/ML

## Overview
This milestone implements a synthesizable INT8 compute core (MAC-based) and a simple memory-mapped interface. Both modules are verified using SystemVerilog testbenches with PASS/FAIL checks.

---

## Directory Structure
